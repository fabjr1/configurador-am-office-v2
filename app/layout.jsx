
export const metadata = {
  title: "Configurador AM OFFICE",
  description: "Sistema de configuração de cadeiras corporativas",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
